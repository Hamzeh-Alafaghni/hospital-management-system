================================================================================
          COMPLETE GITHUB WORKFLOW & GIT COMMANDS GUIDE FOR TEAMS
================================================================================

This guide covers all essential Git and GitHub commands you and your team need for daily
collaboration, setting up new repositories, branching, resolving conflicts, and managing code.

--------------------------------------------------------------------------------
TABLE OF CONTENTS
--------------------------------------------------------------------------------
1. Initial Git Setup & Configuration
2. Starting a Project (New & Existing)
3. The Daily Team Workflow (Branching & Pushing)
4. Pull Requests & Syncing with Team Code
5. Stashing (Saving Temporary Work)
6. Undoing Mistakes & Reverting Changes
7. Resolving Merge Conflicts Step-by-Step
8. Helpful Inspection & History Commands

================================================================================
1. INITIAL GIT SETUP & CONFIGURATION
================================================================================
Run these once when setting up Git on your machine.

# Set your name (appears in commit logs)
git config --global user.name "Your Name"

# Set your email (must match your GitHub account email)
git config --global user.email "your.email@example.com"

# Set default branch name to 'main'
git config --global init.defaultBranch main

# Verify your settings
git config --list


================================================================================
2. STARTING A PROJECT
================================================================================

--- Option A: You are starting a brand new local project ---
# 1. Initialize Git in your project folder
git init

# 2. Add files to staging
git add .

# 3. Create initial commit
git commit -m "feat: initial commit"

# 4. Link local repository to GitHub
git remote add origin https://github.com/username/repository-name.git

# 5. Push local code to GitHub main branch
git push -u origin main


--- Option B: Joining an existing team project ---
# Clone the remote repository to your local machine
git clone https://github.com/username/repository-name.git

# Navigate into the project folder
cd repository-name


================================================================================
3. THE DAILY TEAM WORKFLOW (BRANCHING & COMMITTING)
================================================================================
NEVER work directly on 'main'. Always create a feature branch for your work.

--- Step 1: Switch to main and update your local code ---
git checkout main
git pull origin main

--- Step 2: Create and switch to a new feature branch ---
# Standard naming convention: feature/description, fix/bug-description
git checkout -b feature/add-login-page

--- Step 3: Work on your code and check status ---
# See which files were modified, created, or deleted
git status

# See detailed line-by-line changes made so far
git diff

--- Step 4: Stage your changes ---
# Stage a specific file
git add filename.js

# OR stage all modified files in current folder
git add .

--- Step 5: Commit your changes ---
# Write clear, descriptive commit messages
git commit -m "feat: implement Google login button"

--- Step 6: Push your feature branch to GitHub ---
git push -u origin feature/add-login-page


================================================================================
4. PULL REQUESTS & SYNCING WITH TEAM CODE
================================================================================

--- Step 1: Open a Pull Request on GitHub ---
1. Go to your GitHub repository page in the browser.
2. Click "Compare & pull request" next to your pushed branch.
3. Add a detailed description of your changes and tag teammates for review.
4. Click "Create Pull Request".

--- Step 2: Sync your local feature branch with team updates ---
If teammates merged code into 'main' while you were working on your branch,
bring their changes into your branch before finishing your PR:

# Fetch updated main from GitHub
git fetch origin

# Option A: Merge main into your branch (Standard)
git merge origin/main

# Option B: Rebase your commits on top of main (Cleaner history)
git rebase origin/main


================================================================================
5. STASHING (SAVING TEMPORARY WORK)
================================================================================
Use this when you need to switch branches quickly without committing unfinished work.

# Save unfinished changes to stash
git stash save "WIP: login page styling"

# List all saved stashes
git stash list

# Re-apply the most recent stash and remove it from stash list
git stash pop

# Re-apply a specific stash without deleting it from stash list
git stash apply stash@{0}

# Delete all stashes
git stash clear


================================================================================
6. UNDOING MISTAKES & REVERTING CHANGES
================================================================================

# Unstage a file (keep local changes)
git restore --staged filename.js

# Discard all uncommitted local changes in a file (DESTRUCTIVE)
git restore filename.js

# Discard ALL uncommitted changes in the entire working directory (DESTRUCTIVE)
git reset --hard

# Change the last commit message (before pushing)
git commit --amend -m "feat: updated commit message"

# Undo the last commit, but KEEP your changes staged
git reset --soft HEAD~1

# Safely undo a commit that was already pushed to GitHub (creates a new undo commit)
git revert <commit-hash>


================================================================================
7. RESOLVING MERGE CONFLICTS STEP-BY-STEP
================================================================================
Merge conflicts happen when two people edit the same lines in the same file.

1. When git merge or git pull fails with a conflict:
   git status  (Shows conflicting files under "Unmerged paths")

2. Open conflicting files in VS Code / IDE. Look for conflict markers:
   <<<<<<< HEAD (Your current branch changes)
   Your code...
   =======
   Teammate's code...
   >>>>>>> origin/main (Incoming changes)

3. Edit the file to keep the correct code and remove conflict markers.

4. Stage the resolved files:
   git add filename.js

5. Complete the merge commit:
   git commit -m "fix: resolve merge conflict between main and feature branch"

6. Push the updated code:
   git push origin feature/add-login-page


================================================================================
8. HELPFUL INSPECTION & HISTORY COMMANDS
================================================================================

# View concise commit history
git log --oneline

# View visual branch graph
git log --oneline --graph --all

# List all local branches
git branch

# List all local and remote branches
git branch -a

# Delete a local branch after merging
git branch -d feature/add-login-page

# Force-delete an unmerged branch (CAUTION)
git branch -D feature/add-login-page

# Delete a remote branch on GitHub
git push origin --delete feature/add-login-page

# Check details of remote links
git remote -v